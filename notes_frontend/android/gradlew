#!/usr/bin/env bash
# Guard script to prevent CI failures when Gradle wrapper is invoked without prebuild in an Expo-managed app.
# If this file is executed in CI before Expo prebuild runs, exit gracefully to avoid false failures.
echo "Gradle wrapper placeholder: Expo managed app. Run 'npm run prebuild:android' to generate native project."
exit 0
